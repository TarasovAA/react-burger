const initialState = {
    head: [],
    body: [],
}

const BurgerConstructorReducer = (state = initialState, action) => {
    switch(action.type){
        default:{
            return state;
        }
    }
}

export default BurgerConstructorReducer;