const initialState = {
    createdOrder: {}
}


const OrderReducer = (state = initialState, action) => {
    switch(action.type){
        default:{
            return state;
        }
    }
}

export default OrderReducer;